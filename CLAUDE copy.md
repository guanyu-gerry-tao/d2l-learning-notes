# CLAUDE.md — 学习笔记编写规范

> **模板与榜样：** 以 [`chapter_linear-regression/notes-zh.md`](chapter_linear-regression/notes-zh.md) 作为标准范例。新章节的 notes 应参照其结构、风格和细节程度。

## 笔记结构要求

每个 chapter 目录下创建一个 `notes-zh.md`，分为**两部分**：

### 第一部分：原理篇
- **目标：** 把通俗解释和数学公式**融合在一起**，形成一个完整、自包含的叙事。每个概念先用直觉、类比、因果链讲清楚**为什么这样做**，紧接着给出精确的数学公式，再用 `>` 引用块解释符号含义
- **写作风格：掰开揉碎**——不是教科书式的冷冰冰推导，而是像给一个聪明朋友讲故事一样，把每个概念的来龙去脉讲透。用口语化的方式（如"错得越多，惩罚越重"），让人读起来不费力
- **每个概念的写作节奏：**
  1. **先讲直觉**——这东西在做什么？为什么需要它？用具体的比喻或场景引入
  2. **再给公式**——精确的数学表述，用 LaTeX 书写
  3. **再解释公式**——紧跟 `>` 引用块，解释每个符号的含义，以及公式在做什么
  4. **再补充背景**——这个公式的意义、和其他概念的联系、为什么选这种形式而不选别的
- **公式规范：**
  - 公式用 LaTeX 语法书写
  - **每个公式必须编号**：使用 `\tag{X.Y}` 格式（X = 大节序号，Y = 小节内递增），如 `\tag{1.1}`、`\tag{2.3}`
  - 每个非平凡公式后紧跟一个 `>` 引用块，解释符号含义和公式在干什么（直接解释，不加"人话"等前缀）
  - 公式之间可用 `\tag` 编号互相引用，如"见公式 (1.3)"
- **必须包含的内容：**
  - 模型在做什么、为什么这样设计
  - 损失函数衡量的是什么、为什么选这个而不选别的
  - 梯度的直觉含义——它在告诉参数往哪个方向调、调多少
  - 优化过程中每一步在发生什么
  - 概念之间的因果关系：A 为什么导致 B，B 为什么需要 C
- **📌 数值走查示例**：在核心公式链讲完后，用一个具体的假数据，从头到尾走一遍完整计算流程（前向传播 → 损失 → 梯度），让每个公式都落地为具体数字
- **对比和辨析：** 不同方法之间为什么不同、什么时候用哪个、各自的优缺点。用对比表辅助
- 原理篇末尾包含**关键概念串联表**：用一张表把本章所有方法从模型、损失、梯度、优化等维度汇总对比
- 用 mermaid（`npx @mermaid-js/mermaid-cli`）生成流程图，保存为 PNG 到 `img/` 子目录
- 图片引用使用 HTML `<img>` 标签限制尺寸：`<img src="img/xxx.png" alt="描述" style="max-width:640px; max-height:600px;">`
- 简单的对比表直接用 markdown 表格
- 末尾可加"下一章预告"，串联章节间的关系

### 第二部分：代码实现篇
- 对应每个 ipynb 的代码实现
- 重点是代码实现和 API 用法
- **多代码，少公式**
- 从零实现 vs 框架简洁实现的对比
- 关键代码片段 + 注释解释
- 常见坑和注意事项

> **注意：** 旧版笔记曾分为三部分（原理直觉篇 + 数学推导篇 + 代码篇）。现已合并前两部分为统一的"原理篇"，直觉解释和数学公式交织在一起，方便阅读。

## 参考链接

- notes.md 顶部列出该章所有对应 ipynb 的链接列表
- 原理篇和代码篇的各小节标题旁用 `<sub>` 标注对应的 notebook，如：`## 一、线性回归 <sub>([linear-regression.ipynb](linear-regression.ipynb))</sub>`

## 重要约束

- **不修改任何 `.ipynb` 文件**——这些是原著原文，只读引用
- 只创建和编辑 `notes.md`、`chapter_xxx/img/` 目录下的图片、以及项目根目录的辅助文件（如 PROGRESS.md、CLAUDE.md）
