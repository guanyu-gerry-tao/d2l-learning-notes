# CLAUDE.md — 学习笔记编写规范

> **模板与榜样：** 以 [`chapter_linear-regression/notes-zh.md`](chapter_linear-regression/notes-zh.md) 作为标准范例。新章节的 notes 应参照其结构、风格和细节程度。

## 笔记结构要求

每个 chapter 目录下创建一个 `notes-zh.md`，分为**三部分**：

### 第一部分：原理直觉篇（Intuition）
- **目标：** 用纯自然语言把本章的原理**真正讲透**，深度和第二部分（数学篇）对齐，但**零公式、零代码**
- **不是概览，不是摘要**——而是把每一个概念、每一步推理都用直觉、类比、因果链讲清楚，让一个聪明但没学过数学的人读完后**真正理解为什么这样做、机制是什么**
- **不是把公式翻译成文字**（比如"预测值等于权重乘特征加偏置"这种就是翻译，不要这样做）。而是讲清楚**背后的道理**（比如"每个特征对结果的影响力不同，模型要学会给每个特征分配一个重要程度，最后把它们的加权影响汇总起来做预测"）
- **讲到什么深度：** 第二部分数学篇涵盖的每一个概念，在这里都必须有对应的自然语言解释。包括但不限于：
  - 模型在做什么、为什么这样设计
  - 损失函数衡量的是什么、为什么选这个而不选别的
  - 梯度的直觉含义——它在告诉参数往哪个方向调、调多少
  - 优化过程中每一步在发生什么
  - 概念之间的因果关系：A 为什么导致 B，B 为什么需要 C
- **必须包含具体场景走查：** 用一个具体的假场景（如房价预测、猫狗分类），从头到尾用自然语言走一遍完整流程——不写任何数字计算，但要讲清楚每一步发生了什么、信息怎么流动、模型的"想法"是什么
- **结构与第二部分严格对齐：** 第二部分数学篇用什么章节顺序、什么小节划分，第一部分就用同样的顺序和划分。读者可以逐节对照阅读——先读直觉篇理解"为什么"，再读数学篇看精确表述
- **对比和辨析：** 不同方法之间为什么不同、什么时候用哪个、各自的优缺点。用对比表辅助
- 用 mermaid（`npx @mermaid-js/mermaid-cli`）生成流程图，保存为 PNG 到 `img/` 子目录
- 图片引用使用 HTML `<img>` 标签限制尺寸：`<img src="img/xxx.png" alt="描述" style="max-width:640px; max-height:600px;">`
- 简单的对比表直接用 markdown 表格
- 末尾可加"下一章预告"，串联章节间的关系
- **绝对禁止：** LaTeX 公式、代码块、变量符号（如 $w$、$x$）。可以用"权重"、"特征"等中文术语

### 第二部分：数学推导篇（Math）
- 重点讲解数学原理、公式推导、概念解释
- **多公式，少代码**
- 公式用 LaTeX 语法书写（方便在 Markdown 渲染器中查看）
- **每个公式必须编号**：使用 `\tag{X.Y}` 格式（X = 大节序号，Y = 小节内递增），如 `\tag{1.1}`、`\tag{2.3}`
- 每个非平凡公式后紧跟一个 `>` 引用块，解释符号含义和公式在干什么（直接解释，不加"人话"等前缀）
- 公式之间可用 `\tag` 编号互相引用，如"见公式 (1.3)"
- 概念之间的关系和对比，适时插入**对比表**
- 原理篇末尾包含**关键概念串联表**：用一张表把本章所有方法从模型、损失、梯度、优化等维度汇总对比
- **📌 数值走查示例**：在核心公式链讲完后，用一个具体的假数据（如猫/狗/鸟分类），从头到尾走一遍完整计算流程（前向传播 → 损失 → 梯度），让每个公式都落地为具体数字

### 第三部分：代码实现篇
- 对应每个 ipynb 的代码实现
- 重点是代码实现和 API 用法
- **多代码，少公式**
- 从零实现 vs 框架简洁实现的对比
- 关键代码片段 + 注释解释
- 常见坑和注意事项

## 参考链接

- notes-zh.md 顶部列出该章所有对应 ipynb 的链接列表
- 原理篇和代码篇的各小节标题旁用 `<sub>` 标注对应的 notebook，如：`## 一、线性回归 <sub>([linear-regression.ipynb](linear-regression.ipynb))</sub>`

## 重要约束

- **不修改任何 `.ipynb` 文件**——这些是原著原文，只读引用
- 只创建和编辑 `notes.md`、`notes-zh.md`、`chapter_xxx/img/` 目录下的图片、以及项目根目录的辅助文件（如 PROGRESS.md、CLAUDE.md）
